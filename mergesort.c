#include<stdio.h>
void merge(int lb,int ub,int mid,int arr[]){
    int i,j,k;
    int n1 = mid-lb +1;
    int n2 = ub -  mid;
    int a[n1],b[n2];
    for( i = 0;i<mid;i++){
        a[i] = arr[lb + mid];
    }
    for(j=0;i<)
}